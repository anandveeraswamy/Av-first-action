name: First Workflow
on: workflow_dispatch # manual trigger the workflow
jobs: 
  first-job: 
    runs-on: ubuntu-latest
    steps: 
     - name: Print greeting
       run: "Hello World"
     - name: Print goodbye
       run: echo "Done - bye!"
